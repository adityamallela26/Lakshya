name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Unzip project files (if uploaded as a zip instead of loose files)
        run: |
          if [ -f Lakshya-Android.zip ]; then
            echo "Found Lakshya-Android.zip, extracting..."
            unzip -o Lakshya-Android.zip -d .
          else
            echo "No zip found, assuming files were uploaded individually."
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install dependencies
        run: npm install

      - name: Build web app (Vite)
        run: npx vite build

      - name: Sync Capacitor Android project
        run: npx cap sync android

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: lakshya-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
